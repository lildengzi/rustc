# rustc

rustc —— 让你用 Rust 的语法，体验 C++ 的崩溃。编译全部通过，运行时看缘分。

> 这是一款整活 crate：公开 API 与标准库**同名同貌**（`String`、`Vec`、`Box`、
> `Rc`、`spawn`、`vec!`），`use rustc::*` 之后写出来的代码和普通 Rust 一模一样，
> 但底层全是用 `unsafe` 实现的**真实未定义行为**（悬垂引用、迭代器失效、
> 双重释放、数据竞争）——不是模拟，是真的。**千万不要在生产环境使用。**
